# 🧩 Components (컴포넌트 스타일)
> 재사용 가능한 UI 컴포넌트 설계 가이드

---

## 🎯 컴포넌트 스타일 가이드 요청

```
"우리 앱의 공통 UI 컴포넌트 스타일을 정의해줘:

1. 버튼 (Button)
   - Primary, Secondary, Outline, Text 버튼
   - 크기: Large, Medium, Small
   - 상태: Default, Hover, Active, Disabled

2. 입력 필드 (Input)
   - 텍스트 입력, 검색, 비밀번호
   - 상태: Default, Focus, Error, Disabled

3. 카드 (Card)
   - 기본 카드, 이미지 카드, 리스트 카드

4. 모달/팝업
5. 토스트/스낵바

각 컴포넌트의 padding, border-radius, shadow 값을 포함해줘."
```

---

## 🔘 버튼 컴포넌트

```
"버튼 컴포넌트 스타일을 상세히 정의해줘:

| 속성 | Primary | Secondary | Outline | Text |
|------|---------|-----------|---------|------|
| 배경색 | ? | ? | transparent | transparent |
| 텍스트색 | ? | ? | ? | ? |
| 보더 | none | none | 1px solid | none |
| hover | ? | ? | ? | ? |

크기별:
- Large: h-12, px-6, text-base
- Medium: h-10, px-4, text-sm
- Small: h-8, px-3, text-xs"
```

---

## 📝 입력 필드 컴포넌트

```
"입력 필드 컴포넌트를 정의해줘:

기본 스타일:
- 높이: 48px
- 패딩: 12px 16px
- 보더: 1px solid #E5E7EB
- 보더 radius: 8px

상태별:
- Default: 기본 보더
- Focus: Primary 색상 보더 + shadow
- Error: 빨간색 보더 + 에러 메시지
- Disabled: 회색 배경 + 비활성 커서"
```

---

## 🃏 카드 컴포넌트

```
"카드 컴포넌트 변형을 정의해줘:

1. 기본 카드
   - padding: 16px
   - border-radius: 12px
   - shadow: sm

2. 이미지 카드
   - 상단 이미지 + 하단 콘텐츠

3. 리스트 아이템 카드
   - 가로 방향 레이아웃
   - 썸네일 + 텍스트"
```

---

## 🔔 알림/피드백 컴포넌트

```
"알림 및 피드백 컴포넌트를 정의해줘:

1. 토스트 (Toast)
   - 위치: 상단 중앙 또는 하단
   - 타입: success, error, warning, info
   - 자동 닫힘: 3초

2. 모달 (Modal)
   - 배경 딤 처리
   - 닫기 버튼
   - 확인/취소 버튼

3. 바텀 시트 (Bottom Sheet)
   - 모바일용 하단 팝업"
```

---

## 🔧 컴포넌트 프롭스 정의

```typescript
// Button 타입 정의 예시
interface ButtonProps {
  variant: 'primary' | 'secondary' | 'outline' | 'text';
  size: 'lg' | 'md' | 'sm';
  disabled?: boolean;
  loading?: boolean;
  fullWidth?: boolean;
  onClick?: () => void;
  children: React.ReactNode;
}
```

---

[목차로 돌아가기](./00_INDEX.md)
