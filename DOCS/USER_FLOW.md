# 🗺️ User Flow & Routes (사용자 플로우)

> 수다방(Sudabang) 앱의 페이지 구조 및 사용자 이동 흐름

---

## 1. 사이트맵 (Sitemap)

### 🔓 Public (비로그인)
* `/` : 랜딩 페이지 (인트로)
* `/login` : 로그인 페이지
* `/signup` : 회원가입 페이지

### 🔒 Private (로그인 필요)
* `/home` : 메인 홈 (피드/대시보드) (예정)
* `/rooms` : 채팅방 목록
  * `/rooms/create` : (Modal) 채팅방 생성
  * `/rooms/[id]` : 채팅방 상세 (대화 화면)
* `/goals` : 목표 관리 (대시보드)
* `/profile` : 내 프로필 (예정)
* `/settings` : 설정 (예정)

---

## 2. 주요 플로우 (User Journeys)

### 회원가입 및 온보딩
1. **진입**: `/login` → "회원가입" 클릭 → `/signup`
2. **정보 입력**: 이름, 이메일, 비밀번호 입력
3. **완료**: Firebase Auth 생성 + Firestore User 문서 생성 → `/` (홈) 이동

### 채팅방 참여 및 생성
1. **탐색**: `/rooms` 진입 → 카테고리 필터(`Select`)로 관심사 선택
2. **생성**: **"+ 방 만들기"** 버튼 클릭 → `CreateRoomModal` 팝업
   - 정보 입력(이름, 카테고리) → 생성 완료 → 목록 자동 갱신
3. **참여**: 채팅방 카드 클릭 → `/rooms/[id]` 이동

### 목표 설정
1. **대시보드**: `/goals` 진입 → 현재 진행률 확인
2. **추가**: **"+ 새 목표"** 클릭 → `CreateGoalModal` 팝업
   - 목표명, 카테고리, 마감일 설정
3. **관리**: 목표 카드에서 진행률 조절(`+10%`, `-10%`) 또는 삭제

---

## 3. 라우팅 상수 (Constants)
`src/constants/index.ts`에 정의됨

```typescript
export const ROUTES = {
  HOME: '/',
  LOGIN: '/login',
  SIGNUP: '/signup',
  ONBOARDING: '/onboarding',
  FEED: '/feed',
  ROOMS: '/rooms',
  ROOM_DETAIL: (id: string) => `/rooms/${id}`,
  GOALS: '/goals',
  PROFILE: '/profile',
  SETTINGS: '/settings',
};
```
